# Зависимости прототипа otchetTK (ТЗ, п. 7: Python 3.11+)

# Ядро
httpx>=0.27          # обращения к OpenAI-совместимым API (внешним и локальным)
PyYAML>=6.0          # конфигурация
jsonschema>=4.21     # валидация ответов звеньев цепи промптов (ТЗ, п. 5.3)
python-docx>=1.1     # извлечение текста из .docx (ТЗ, пп. 3.1-3.2)
pypdf>=4.2           # извлечение текста из .pdf с текстовым слоем

# Тесты
pytest>=8.0

# Генерация тестовых данных (scripts/make_test_data.py)
fpdf2>=2.7
